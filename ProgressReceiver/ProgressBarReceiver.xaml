<UserControl x:Class="ImageNexus.BenScharbach.Common.ProgressBar.Simple.Receiver.ProgressBarReceiver"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="12" d:DesignWidth="25" 
             Name="UcProgressReceiverBar">
    <Grid Name="GridContainer" Height="{Binding ElementName=UcProgressReceiverBar, Path=ActualHeight}" Width="{Binding ElementName=UcProgressReceiverBar, Path=ActualWidth}" >
        <ProgressBar Name="PbReceiver" Height="{Binding ElementName=GridContainer, Path=ActualHeight}" HorizontalAlignment="Stretch" Margin="0" VerticalAlignment="Stretch" Width="{Binding ElementName=GridContainer, Path=ActualWidth}" />
    </Grid>
</UserControl>
